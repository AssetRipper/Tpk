name: MakeTpk

on:
   push:
    branches: [ master ]
   pull_request:
    branches: [ master ]
   workflow_dispatch:
  

jobs:
    build:
     runs-on: ubuntu-latest
     steps:
     - name: Checkout TypeTreeCompression Repository
       uses: actions/checkout@v3
     - name: Setup .NET
       uses: actions/setup-dotnet@v2
       with:
         dotnet-version: 6.0.x
     - name: Build
       run: dotnet build /p:Configuration=Release
       working-directory: ./TpkCreator/
     - name: Checkout TypeTreeDumps Repository
       uses: actions/checkout@v3
       with:
        repository: 'AssetRipper/TypeTreeDumps'
        path: TypeTreeDumps
     - name: Make files executable
       shell: bash
       run: chmod +x ./TpkCreator/bin/Release/net6.0/TpkCreator
     - name: Make tpk file
       shell: bash
       run: ./TpkCreator/bin/Release/net6.0/TpkCreator ./TypeTreeDumps/InfoJson
     - name: Upload file
       uses: actions/upload-artifact@v2
       with:
         name: uncompressed_file
         path: /home/runner/work/TypeTreeCompression/TypeTreeCompression/uncompressed.tpk
         if-no-files-found: error
     - name: Upload file
       uses: actions/upload-artifact@v2
       with:
         name: lz4_file
         path: /home/runner/work/TypeTreeCompression/TypeTreeCompression/compressed.tpk
         if-no-files-found: error
      